import { signInWithEmail } from '../../src/views/signInWithEmail';

jest.mock('../../src/firebaseConfig.js');
jest.mock('../../src/authFunctions.js');

describe('signInWithEmail', () => {
  it('', () => {
    const result = signInWithEmail();
  });
});
